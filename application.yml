spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  application:
    name: microservice-config-dev # 开发环境
server:
  port: 8201    
---
spring:
  profiles: test
  application:
    name: microservice-config-test # 测试环境
server:
  port: 8202             